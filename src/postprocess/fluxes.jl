export get_observed_bolometric_fluxes

function get_observed_bolometric_fluxes(configurations::VacuumOTEConfigurations)

    #get_Q_factors(rays, spacetime, model)
    #get_local_emission(rays, model)
    #return Q**4*Fem

end

function get_observed_specific_fluxes(configurations::VacuumOTEConfigurations, frequencies)

    #get_Q_factors(rays, spacetime, model)
    #get_local_emission(rays, model)
    #return Q**4*Fem

end

function set_Q_factors!(Q_factors, rays, spacetime, model)


    # for ray in rays:

    #view initial/final position, momentum

    # observer_four_velocity = get_static_observer_four_velocity(initial_positions)
    # em_four_velocity = get_emitter_four_velocity(initial_positions, model)
    
    # observer_metric = set_metric_(initial_positions, spacetime) 
    # emitter_metric = set_metric_(final_positions, spacetime)
    
    # Q = scalar_product(initial_momentum, observer_four_velocity, observer_metric)/scalar_product(final_momentum, emitter_four_velocity, emitter_metric

    #return Q

end
